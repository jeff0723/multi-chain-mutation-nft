"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.getAddress = void 0;
const addresses_1 = require("@zetachain/addresses");
const hardhat_1 = require("hardhat");
const MissingZetaNetworkError = new Error("ZETA_NETWORK is not defined, please set the environment variable (e.g.: ZETA_NETWORK=athens <command>)");
const getAddress = (address, { customNetworkName, customZetaNetwork, } = {}) => {
    const { name: _networkName } = hardhat_1.network;
    const networkName = customNetworkName || _networkName;
    const { ZETA_NETWORK: _ZETA_NETWORK } = process.env;
    const zetaNetwork = customZetaNetwork || _ZETA_NETWORK;
    if (!zetaNetwork)
        throw MissingZetaNetworkError;
    return (0, addresses_1.getAddress)({ address, networkName, zetaNetwork });
};
exports.getAddress = getAddress;
